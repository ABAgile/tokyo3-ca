// Package common holds cert lifecycle helpers shared between certd
// (the server binary) and cert-agentd (the workload-side agent) —
// e.g., TTL arithmetic, KeyID schema constants, cert-template builders.
package common
